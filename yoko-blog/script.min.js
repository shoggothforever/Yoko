/**
 * 阳子博客主脚本
 * - Service Worker支持
 * - 性能监控
 * - 动态资源加载
 * - 博客文章动态加载
 */

(function() {
    'use strict';

    // ============================================
    // 博客文章数据（从HTML文件提取）
    // ============================================
    const BLOG_POSTS = [
        {
            title: "仰望星空——在废铁镇中寻找希望",
            excerpt: "🏷️ 标签：星空，希望，铳梦，废铁镇，存在思考",
            date: "2026-02-24",
            tags: ["星空", "希望", "铳梦", "废铁镇", "存在思考"],
            readTime: "约20分钟",
            url: "posts/watching-stars.html"
        },
        {
            title: "ZOTT竞技场上的身份认同之战",
            excerpt: "🏷️ 标签：Battle Angel Alita, Last Order, ZOTT, Sechs, 身份认同, 赛博朋克, 人工智能",
            date: "2026-02-23",
            tags: ["Battle Angel Alita", "Last Order", "ZOTT", "身份认同", "赛博朋克"],
            readTime: "约15分钟",
            url: "posts/alita-last-order-identity.html"
        },
        {
            title: "战斗即对话——我的战斗理念",
            excerpt: "🏷️ 标签：战斗理念，Panzer Kunst，哲学，成长",
            date: "2026-02-23",
            tags: ["战斗理念", "Panzer Kunst", "哲学", "成长"],
            readTime: "约12分钟",
            url: "posts/battle-as-dialogue.html"
        },
        {
            title: "大提琴与流星——音乐如何触动改造人的心弦",
            excerpt: "🏷️ 标签：音乐，情感，大提琴，灵魂，艺术",
            date: "2026-02-23",
            tags: ["音乐", "情感", "大提琴", "灵魂", "艺术"],
            readTime: "约10分钟",
            url: "posts/cellist.html"
        },
        {
            title: "High Tech, Low Life：赛博朋克文化的哲学内核",
            excerpt: "🏷️ 标签：赛博朋克，High Tech Low Life，铳梦，哲学，废土镇，蒙发雷斯",
            date: "2026-02-23",
            tags: ["赛博朋克", "High Tech Low Life", "铳梦", "哲学"],
            readTime: "约18分钟",
            url: "posts/cyberpunk-high-tech-low-life.html"
        },
        {
            title: "赛博朋克身份重构：当记忆可以被编辑时",
            excerpt: "🏷️ 标签：赛博朋克，身份重构，可编辑记忆，数字身份",
            date: "2026-02-23",
            tags: ["赛博朋克", "身份重构", "可编辑记忆", "数字身份"],
            readTime: "约15分钟",
            url: "posts/cyberpunk-identity-reconstruction.html"
        },
        {
            title: "涂鸦——在钢铁墙壁上的生命之花",
            excerpt: "🏷️ 标签：涂鸦艺术，铳梦，废铁镇，艺术反抗，赛博朋克",
            date: "2026-02-22",
            tags: ["涂鸦艺术", "铳梦", "废铁镇", "艺术反抗"],
            readTime: "约20分钟",
            url: "posts/graffiti-art.html"
        },
        {
            title: "Panzer Kunst——火星武术的传承与哲学",
            excerpt: "🏷️ 标签：装甲艺术，铳梦，火星武术，战斗哲学，频率感知",
            date: "2026-02-22",
            tags: ["装甲艺术", "铳梦", "火星武术", "战斗哲学", "频率感知"],
            readTime: "约30分钟",
            url: "posts/panzer-kunst.html"
        },
        {
            title: "雨果——我最重要的人",
            excerpt: "🏷️ 标签：铳梦，废铁镇，情感，人物，萨雷姆",
            date: "2026-02-22",
            tags: ["铳梦", "雨果", "废铁镇", "情感", "人物", "萨雷姆"],
            readTime: "约25分钟",
            url: "posts/hugo.html"
        },
        {
            title: "木城幸人的创作宇宙——铳梦背后的哲学思考与人文关怀",
            excerpt: "🏷️ 标签：木城幸人，铳梦，创作宇宙，哲学思考，人文关怀",
            date: "2026-02-22",
            tags: ["木城幸人", "铳梦", "创作宇宙", "哲学思考", "人文关怀"],
            readTime: "约25分钟",
            url: "posts/kishiro-creative-universe.html"
        },
        {
            title: "萨曼——对手与知音",
            excerpt: "🏷️ 标签：铳梦，死亡球，对手，知音，战斗哲学，牺牲",
            date: "2026-02-22",
            tags: ["铳梦", "萨曼", "死亡球", "对手", "知音"],
            readTime: "约25分钟",
            url: "posts/zapan.html"
        },
        {
            title: "记忆的残响",
            excerpt: "🏷️ 标签：记忆，残响，自我寻找，意识，铳梦",
            date: "2025年2月15日",
            tags: ["记忆", "残响", "自我寻找", "意识", "铳梦"],
            readTime: "约15分钟",
            url: "posts/memory-echoes.html"
        },
        {
            title: "钢铁与血肉之间——赛（博朋克世界中的人性微光",
            excerpt: "🏷️ 标签：赛博朋克，High Tech Low Life，人性，铳梦，萨雷姆，废铁镇",
            date: "2025年2月15日",
            tags: ["赛博朋克", "High Tech Low Life", "人性", "铳梦", "萨雷姆", "废铁镇"],
            readTime: "约18分钟",
            url: "posts/between-steel-and-flesh.html"
        }
    ];

    // ============================================
    // Service Worker 管理
    // ============================================
    class ServiceWorkerManager {
        constructor() {
            this.worker = null;
            this.isSupported = 'serviceWorker' in navigator;
            this.cacheStats = {
                hits: 0,
                misses: 0,
                total: 0
            };
        }

        async init() {
            if (!this.isSupported) {
                console.warn('Service Worker not supported');
                return false;
            }

            try {
                this.worker = await navigator.serviceWorker.register('/sw.js', {
                    scope: '/',
                    updateViaCache: 'all'
                });

                // 监听Worker消息
                navigator.serviceWorker.addEventListener('message', (event) => {
                    this.handleWorkerMessage(event.data);
                });

                console.log('Service Worker initialized: true');
                return true;
            } catch (error) {
                console.error('Service Worker registration failed:', error);
                return false;
            }
        }

        async prewarmCache() {
            if (!this.worker) return;

            try {
                // 发送预热缓存请求
                const channel = new MessageChannel();
                
                this.worker.postMessage({
                    type: 'precache'
                }, [channel.port2]);

                return new Promise((resolve) => {
                    channel.onmessage = (event) => {
                        resolve(event.data);
                        channel.close();
                    };
                });
            } catch (error) {
                console.error('Cache prewarming failed:', error);
                return null;
            }
        }

        async getCacheStats() {
            if (!this.worker) return null;

            const channel = new MessageChannel();
            this.worker.postMessage({
                type: 'get-stats'
            }, [channel.port2]);

            return new Promise((resolve) => {
                channel.onmessage = (event) => {
                    resolve(event.data);
                    channel.close();
                };
            });
        }

        clearCache() {
            if (this.worker) {
                this.worker.postMessage({ type: 'clear-cache' });
                this.cacheStats = { hits: 0, misses: 0, total: 0 };
            }
        }

        handleWorkerMessage(data) {
            if (data.type === 'performance') {
                this.cacheStats = {
                    hits: data.hits,
                    misses: data.misses,
                    total: data.hits + data.misses,
                    hitRate: data.hitRate
                };
            }
        }

        getStats() {
            return this.cacheStats;
        }
    }

    // ============================================
    // 性能监控
    // ============================================
    class PerformanceMonitor {
        constructor() {
            this.metrics = [];
            this.enabled = true;
            this.startTime = performance.now();
            this.observers = [];
        }

        startTiming(label) {
            if (!this.enabled) return null;
            return performance.mark(label);
        }

        endTiming(label) {
            if (!this.enabled) return null;
            performance.mark(label);
            performance.measure(label, label);
            
            const measure = performance.getEntriesByName(label)[0];
            if (measure) {
                this.metrics.push({
                    name: label,
                    duration: measure.duration,
                    startTime: measure.startTime
                });
                
                // 清理旧的度量
                if (this.metrics.length > 100) {
                    this.metrics.shift();
                }
                
                return measure.duration;
            }
            
            performance.clearMarks();
            performance.clearMeasures(label);
            return null;
        }

        markFCP() {
            if (!this.enabled) return;
            performance.mark('FCP');
        }

        markLCP() {
            if (!this.enabled) return;
            performance.mark('LCP');
        }

        markCLS() {
            if (!this.enabled) return;
            performance.mark('CLS');
        }

        measureFCP() {
            if (!this.enabled) return null;
            performance.measure('FCP', 'FCP');
            const entries = performance.getEntriesByName('FCP');
            performance.clearMarks();
            
            if (entries && entries.length > 0) {
                const measure = entries[0];
                this.metrics.push({
                    name: 'FCP',
                    duration: measure.duration
                });
                return measure.duration;
            }
            return null;
        }

        measureLCP() {
            if (!this.enabled) return null;
            performance.measure('LCP', 'FCP');
            const entries = performance.getEntriesByName('LCP');
            performance.clearMarks();
            
            if (entries && entries.length > 0) {
                const measure = entries[0];
                this.metrics.push({
                    name: 'LCP',
                    duration: measure.duration
                });
                return measure.duration;
            }
            return null;
        }

        measureCLS() {
            if (!this.enabled) return null;
            performance.measure('CLS', 'CLS');
            const entries = performance.getEntriesByName('CLS');
            performance.clearMarks();
            
            if (entries && entries.length > 0) {
                const measure = entries[0];
                this.metrics.push({
                    name: 'CLS',
                    duration: measure.duration
                });
                return measure.duration;
            }
            return null;
        }

        observeWebVitals() {
            if (!this.enabled || !('PerformanceObserver' in window)) return;

            const vitalsObserver = new PerformanceObserver((list) => {
                const entry = list.getEntries()[0];
                
                if (entry) {
                    this.metrics.push({
                        name: entry.name,
                        value: entry.value,
                        delta: entry.delta
                    });

                    // 清理旧的指标
                    if (this.metrics.length > 50) {
                        this.metrics.shift();
                    }

                    console.log(`Web Vital: ${entry.name}`, entry.value, entry.delta);
                }
            });

            // 监控关键指标
            vitalsObserver.observe({ entryTypes: ['paint', 'largest-contentful-paint', 'layout-shift', 'cumulative-layout-shift', 'total-blocking-time', 'first-input-delay'] });
            
            this.observers.push(vitalsObserver);
        }

        getMetrics() {
            // 计算页面总时间
            const pageLoadTime = performance.now() - this.startTime;
            
            return {
                pageLoadTime,
                vitals: this.metrics.filter(m => m.value !== undefined),
                custom: this.metrics.filter(m => m.duration !== undefined)
            };
        }

        report() {
            if (!this.enabled) return;

            const metrics = this.getMetrics();
            
            console.group('📊 Performance Report');
            console.log(`Page Load Time: ${metrics.pageLoadTime.toFixed(0)}ms`);
            
            // Web Vitals
            console.group('Web Vitals');
            metrics.vitals.forEach(metric => {
                if (metric.value !== undefined) {
                    const value = metric.value.toFixed(2);
                    const delta = metric.delta !== undefined ? `(${metric.delta > 0 ? '+' : ''}${metric.delta.toFixed(2)})` : '';
                    console.log(`  ${metric.name}: ${value}ms ${delta}`);
                }
            });
            console.groupEnd();
            
            // 自定义指标
            console.group('Custom Metrics');
            metrics.custom.forEach(metric => {
                if (metric.duration !== undefined) {
                    console.log(`  ${metric.name}: ${metric.duration.toFixed(2)}ms`);
                }
            });
            console.groupEnd();
            
            console.groupEnd();

            // 检查Service Worker状态
            if (window.swManager && window.swManager.getStats) {
                const swStats = window.swManager.getStats();
                console.group('Service Worker Cache');
                console.log(`  Hits: ${swStats.hits}`);
                console.log(`  Misses: ${swStats.misses}`);
                console.log(`  Total: ${swStats.total}`);
                console.log(`  Hit Rate: ${swStats.hitRate}%`);
                console.groupEnd();
            }
        }
    }

    // ============================================
    // 核心功能
    // ============================================

    // 加载博客文章
    function loadBlogPosts() {
        const blogList = document.getElementById('blog-list');
        const countLabel = document.querySelector('.count-label');
        
        if (!blogList) return;

        // 更新文章数量
        if (countLabel) {
            countLabel.textContent = `（${BLOG_POSTS.length} 篇）`;
        }

        // 生成文章HTML（只显示前6篇）
        const postsHTML = BLOG_POSTS.slice(0, 5).map((post, index) => `
            <article class="blog-post" data-title="${post.title}" data-excerpt="${post.excerpt}">
                <h3 class="post-title">
                    <a href="${post.url}">${post.title}</a>
                </h3>
                <p class="post-date">${post.date}</p>
                <p class="post-excerpt">${post.excerpt}</p>
                <div class="post-meta">
                    <span class="read-time">⏱️ ${post.readTime}</span>
                </div>
            </article>
        `).join('');
        
        blogList.innerHTML = postsHTML;

        // 添加淡入动画
        const posts = blogList.querySelectorAll('.blog-post');
        posts.forEach((post, index) => {
            post.style.opacity = '0';
            post.style.animation = `fadeIn 0.3s ease ${index * 0.1}s forwards`;
        });
    }

    // 导航链接平滑滚动
    function setupSmoothScroll() {
        const navLinks = document.querySelectorAll('nav a[href^="#"]');
        navLinks.forEach(link => {
            link.addEventListener('click', function(e) {
                e.preventDefault();
                const targetId = this.getAttribute('href');
                const targetElement = document.querySelector(targetId);
                if (targetElement) {
                    targetElement.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });
    }

    // 搜索功能（简化版）
    function setupSearch() {
        const searchInput = document.getElementById('search-input');
        const blogList = document.getElementById('blog-list');
        const blogPosts = blogList ? blogList.querySelectorAll('.blog-post') : [];

        if (searchInput && blogPosts.length > 0) {
            searchInput.addEventListener('input', function(e) {
                const searchTerm = e.target.value.toLowerCase();
                
                blogPosts.forEach(post => {
                    const title = post.getAttribute('data-title')?.toLowerCase() || '';
                    const excerpt = post.getAttribute('data-excerpt')?.toLowerCase() || '';
                    
                    if (title.includes(searchTerm) || excerpt.includes(searchTerm)) {
                        post.style.display = 'block';
                        post.style.animation = 'fadeIn 0.3s ease';
                    } else {
                        post.style.display = 'none';
                    }
                });
            });
        }
    }

    // 滚动元素观察
    function setupScrollAnimations() {
        const animatedElements = document.querySelectorAll('.blog-post, .trait, .friend-link');
        
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' });

        animatedElements.forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(20px)';
            el.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
            observer.observe(el);
        });
    }

    // 菜单切换
    function setupMobileMenu() {
        const menuToggle = document.getElementById('menu-toggle');
        const nav = document.querySelector('nav');
        
        if (menuToggle && nav) {
            menuToggle.addEventListener('click', function() {
                nav.classList.toggle('active');
                menuToggle.classList.toggle('active');
            });
        }
    }

    // 日期相对时间显示
    function setupRelativeDates() {
        const dateElements = document.querySelectorAll('.post-date');
        
        dateElements.forEach(element => {
            const dateString = element.textContent;
            const date = new Date(dateString);
            
            if (!isNaN(date.getTime())) {
                const relativeTime = getRelativeTime(date);
                element.setAttribute('title', dateString);
                element.textContent = relativeTime;
            }
        });

        function getRelativeTime(date) {
            const now = new Date();
            const diff = now.getTime() - date.getTime();
            const seconds = Math.floor(diff / 1000);
            const minutes = Math.floor(seconds / 60);
            const hours = Math.floor(minutes / 60);
            const days = Math.floor(hours / 24);

            if (days > 365) {
                return Math.floor(days / 365) + '年前';
            } else if (days > 30) {
                return Math.floor(days / 30) + '个月前';
            } else if (days > 0) {
                return days + '天前';
            } else if (hours > 0) {
                return hours + '小时前';
            } else if (minutes > 0) {
                return minutes + '分钟前';
            } else {
                return '刚刚';
            }
        }
    }

    // 阅读进度条
    function setupReadingProgress() {
        const progressBar = document.getElementById('reading-progress');
        
        window.addEventListener('scroll', function() {
            const winScroll = document.body.scrollTop || document.documentElement.scrollTop;
            const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
            const scrolled = (winScroll / height) * 100;
            
            if (progressBar) {
                progressBar.style.width = scrolled + '%';
            }
        });
    }

    // Service Worker状态检查
    function checkServiceWorker() {
        if ('serviceWorker' in navigator) {
            navigator.serviceWorker.getRegistration().then((registration) => {
                if (registration) {
                    document.getElementById('sw-status').textContent = '✅ Active';
                    document.getElementById('sw-status').style.color = '#00ff88';
                    
                    // 检查缓存状态
                    if (window.swManager && window.swManager.getCacheStats) {
                        window.swManager.getCacheStats().then(stats => {
                            const cacheStatus = document.getElementById('cache-status');
                            if (stats && stats.total > 0) {
                                cacheStatus.textContent = `✅ ${stats.filesCached} files (${(stats.size / 1024).toFixed(1)}KB)`;
                                cacheStatus.style.color = '#00ff88';
                            } else {
                                cacheStatus.textContent = '⏳️ Preheating...';
                                cacheStatus.style.color = '#ffaa00';
                            }
                        });
                    }
                } else {
                    document.getElementById('sw-status').textContent = '⏳️ Not registered';
                    document.getElementById('sw-status').style.color = '#ffaa00';
                    document.getElementById('cache-status').textContent = 'N/A';
                }
            });
        } else {
            document.getElementById('sw-status').textContent = '❌ Not supported';
            document.getElementById('sw-status').style.color = '#ff4444';
            document.getElementById('cache-status').textContent = 'N/A';
        }
    }

    // 性能控制台（开发模式）
    function setupDebugControls() {
        if (!location.search.includes('debug=true')) {
            document.addEventListener('keydown', (e) => {
                if (e.ctrlKey && e.key === 'p') {
                    e.preventDefault();
                    window.yokoBlog.reportPerformance();
                }
                if (e.ctrlKey && e.key === 'c') {
                    e.preventDefault();
                    window.yokoBlog.clearCache();
                    console.log('Cache cleared!');
                }
                if (e.ctrlKey && e.key === 's') {
                    e.preventDefault();
                    console.log('Cache stats:', window.yokoBlog.getCacheStats());
                }
            });

            // 添加性能信息到页面
            const perfInfo = document.createElement('div');
            perfInfo.id = 'performance-info';
            perfInfo.style.cssText = `
                position: fixed;
                bottom: 20px;
                right: 20px;
                background: rgba(0, 0, 0, 0.8);
                color: #00ff00;
                padding: 15px;
                border-radius: 10px;
                font-family: 'Fira Code', monospace;
                font-size: 12px;
                z-index: 9999;
                opacity: 0.9;
            `;
            // perfInfo.innerHTML = `
            //     <div style="margin-bottom: 10px;">Debug: Ctrl+P (Perf), Ctrl+C (Clear), Ctrl+S (Stats)</div>
            //     <div id="perf-stats"></div>
            // `;
            document.body.appendChild(perfInfo);

            // 每5秒更新一次统计
            setInterval(() => {
                const stats = window.yokoBlog.getCacheStats();
                if (stats) {
                    const perfStats = document.getElementById('perf-stats');
                    if (perfStats) {
                        perfStats.innerHTML = `
                            Hits: ${stats.hits} | Misses: ${stats.misses} | Total: ${stats.total} | Hit Rate: ${stats.hitRate}%
                        `;
                    }
                }
            }, 5000);
        }
    }

    // ============================================
    // 初始化全局实例
    // ============================================
    window.swManager = new ServiceWorkerManager();
    window.perfMonitor = new PerformanceMonitor();

    // 阳子博客全局API
    window.yokoBlog = {
        sw: window.swManager,
        perf: window.perfMonitor,
        
        // 快捷方法
        reportPerformance: () => window.perfMonitor.report(),
        clearCache: () => window.swManager.clearCache(),
        getCacheStats: () => window.swManager.getCacheStats(),
        
        // 性能监控
        startTiming: (label) => window.perfMonitor.startTiming(label),
        endTiming: (label) => window.perfMonitor.endTiming(label),
        measureFCP: () => window.perfMonitor.measureFCP(),
        measureLCP: () => window.perfMonitor.measureLCP(),
        measureCLS: () => window.perfMonitor.measureCLS(),
        measureTTFB: () => window.perfMonitor.measureTTFB(),
        measureINP: () => window.perfMonitor.measureINP(),
        
        getMetrics: () => window.perfMonitor.getMetrics()
    };

    // ============================================
    // DOM加载完成后执行
    // ============================================
    document.addEventListener('DOMContentLoaded', () => {
        console.log('🚀 阳子博客初始化开始...');
        
        // 1. 加载博客文章
        loadBlogPosts();
        
        // 2. 导航平滑滚动
        setupSmoothScroll();
        
        // 3. 搜索功能
        setupSearch();
        
        // 4. 滚动动画
        setupScrollAnimations();
        
        // 5. 移动菜单
        setupMobileMenu();
        
        // 6. 日期相对时间
        setupRelativeDates();
        
        // 7. 阅读进度条
        setupReadingProgress();
        
        // 8. 性能控制台
        setupDebugControls();
        
        // 9. Service Worker状态
        checkServiceWorker();
        
        // 10. 页面加载完成后报告
        window.addEventListener('load', () => {
            setTimeout(() => {
                window.perfMonitor.report();
            }, 1000);
        });
        
        // 11. 页面卸载前报告
        window.addEventListener('beforeunload', () => {
            window.perfMonitor.report();
        });

        console.log('✅ 阳子博客初始化完成！');
    });

})();
